# TASK-003 Adjustment Log

## What changed
- Replaced generic metro coordinates in `config/cities.yml` with settlement-airport coordinates from `docs/BETTING_INSIGHTS.md`.
- Aligned the KPHL station coordinate in `config/stations.yml` to the same canonical Philadelphia airport coordinate.
- Added regression tests confirming the loaded city registry uses airport coordinates and KDCA NWS point lookup calls `/points/38.8521,-77.0377`.

## Files changed
- `config/cities.yml`
- `config/stations.yml`
- `tests/test_registry_loader.py`
- `tests/test_asos.py`

## Why
Kalshi high-temperature markets settle against airport stations, not generic metro centers. Forecast lookups and persisted city metadata should therefore use the station coordinates so NWS grid selection is tied to the settlement location.

## Caveats
- The live ASOS forecast path already used airport-specific coordinates from `STATION_REGISTRY`; this change primarily fixes the YAML-backed city registry and keeps station config consistent.
- The NWS `/points` API can still return a nearby grid cell rather than an exact observation-station forecast, so station observations remain the settlement truth check.
