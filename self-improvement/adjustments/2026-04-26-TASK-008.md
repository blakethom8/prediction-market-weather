# TASK-008 Adjustment Log

## What changed

- Settled `paperbet_6ff2034fd48d` / `KXHIGHTDC-26APR15-B90.5` as `yes`.
- Settled `paperbet_53a6cfaa8009` / `KXHIGHMIA-26APR15-B79.5` as `no`.
- Inserted matching `ops.paper_bet_reviews` rows and updated the related `ops.calibration_log` rows with actual highs, outcomes, forecast errors, realized PnL, and settlement notes.

## Files changed

- `data/warehouse/weather_markets.duckdb`
- `self-improvement/adjustments/2026-04-26-TASK-008.md`

## Why

The two Apr 15 paper bets were stale in `ops.paper_bets` with `status = 'open'`. `make chief -- settle` could not resolve them because the warehouse is missing matching Apr 15 `core.weather_contracts` and `core.settlement_observations` rows for those tickers, so the manual settlement was applied directly.

## Caveats

- DC outcome note: the task note said the DC high was well below 90.5F, but the verified official CLIDCA high for Apr 15, 2026 was 90F. In this repo, `B90.5` maps to the 90-91F bucket, so the YES bet was settled as a win.
- Miami outcome: official CLIMIA high for Apr 15, 2026 was 81F. `B79.5` maps to the 79-80F bucket, so the YES bet was settled as a loss.
